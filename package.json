{
  "name": "template-rust-pages",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "build": "spago build && parcel build src/index.html --public-url ./",
    "dev": "concurrently \"spago build --watch\" \"parcel src/index.html\""
  },
  "dependencies": {
    "concurrently": "^6.2.1",
    "parcel-bundler": "^1.12.4",
    "purescript": "^0.14.3",
    "spago": "^0.20.3"
  }
}
